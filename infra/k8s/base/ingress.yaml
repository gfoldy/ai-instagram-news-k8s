apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ai-news-ingress
  annotations:
    kubernetes.io/ingress.class: traefik
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: review-ui
                port:
                  number: 80
